# Audit — garanties (EN → /guarantees)

## Net score: SDS − HPC = 10 − 3 = **7**

**Verdict (Layer 14): Clean → low Seasoned (borderline).** Ship after one light pass on the negative-parallelism openers. No structural work.

Audited EN prose ≈ 700 words. Trust/BOFU page; some repetition is a correctness requirement (guarantee triggers + conditions) and is NOT penalized.

### Top tells (verbatim · layer/weight)
- "A guarantee isn't a marketing line. It's a consequence of the model." — Rule 2.1 negative parallelism (3). Load-bearing: the page then actually substantiates the "consequence of the model" claim via the capacity logic, so scored at face value, not doubled aggressively.
- "the guarantees aren't a commercial bet, they're the direct consequence of how we work." — Rule 2.1 (3). Second instance on page → the two together are the page's signature device (corpus-wide, see below).
- "That's the whole logic:" — Rule 2.5 colon-headline (1), minor.
- "An honest guarantee has honest conditions." — mild aphorism (1); redeemed because real conditions follow immediately.

### Human-presence credits
- FAQ2 "That's the one real risk, and we handle it from kick-off" — 16.2 admission naming the genuine failure mode (client teams not participating) rather than hiding it. Load-bearing. (~2 cr)
- Concrete, checkable activation mechanics ("One email is enough. No form, no justification") and a public terms-of-sale link — situated knowledge, not filler. (~1 cr)

### Substance (Layer 5)
Strong PASS. Every guarantee carries an exact trigger + activation condition + precondition table. Specific, falsifiable, contract-anchored. No fabrication. Zero-Knowledge test passes in every section.

### Concrete remediation
- The two Rule 2.1 openers are the only real flags. Fix by asserting one clause, not the antithesis: e.g. replace "A guarantee isn't a marketing line. It's a consequence of the model." with "Our guarantees follow directly from the capacity model: [one sentence stating the mechanism]." Delete the "isn't X" half rather than synonym-swap.

### Repeated devices seen on this page
- Negative parallelism "isn't X … it's Y" (×2) — corpus signature.
- "3 high-ROI use cases" / audit-refund conditions (repeated across corpus; here it is necessary compliance repetition).
- "Book my free diagnostic" CTA + "30 minutes to analyse your workflows."
