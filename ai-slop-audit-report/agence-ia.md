# Anti-AI-Slop Audit — Agence IA (`/agence-ia` → `/ai-consulting`)

## Net score: SDS − HPC = ~ −1
- **Word count (AIM-authored EN prose: hero/intro, section copy, FAQ, GEO, CTA; excl. scaffolding/tables/rationale):** ~760
- **SDS (slop density /1,000w):** ~12
- **HPC (human presence credits /1,000w):** ~13 (hard cap 20)
- **Net:** ≈ **−1**
- **Layer 14 verdict:** **CLEAN — ship.** No automatic-failure class. Zero-Knowledge test passes in every section (named stack, named offices, named founder, measured figures). The dominant tell is corpus-level device repetition, not hollowness. Action: one light pass to de-duplicate the "X, not Y" frame so it stops reading as a tic across the funnel.

## Top tells (verbatim, with layer/weight)
1. **Layer 2.1 — negative parallelism as house style (3 pts base, doubled for recurrence ≈ 6).** The site signature is dense here: h1 `"AI consulting that ships systems, not slides"`; section title `"Systems in production, not demos"`; comparison `"Only one ships systems into production"`; FAQ3 `"advising and building are the same engagement here, not two vendors"`. Each is load-bearing (real contrast), so not hollow — but the *accumulation* is machine-adjacent cadence repeated on every page. **Remediation:** keep the h1; flatten one of the two "systems not demos/slides" restatements to a plain assertion.
2. **Layer 2.5 — colon-headline (2 pts).** `"The goal: a company that runs without us at six months."` One instance, punchy. Borderline; acceptable, but it recurs corpus-wide — flag for synthesis.
3. **Layer 1 Tier A — "leverage" (1 pt).** Card 3 (who): `"strong leverage for AI"`. Noun form, mild, but it's the exact word the ruleset flags; a plainer "strong payoff from AI" would kill it.
4. **Layer 2.4 — rule-of-three (watch, not scored).** `"agents, automations, department copilots"`; `"qualifying a request, preparing a document, synthesising exchanges"`; `"sales, legal, support, or finance"`. Each triad is concrete/enumerative (a real list, not cadence filler), so no penalty — but three triads on one short page is near the machine-cadence line.

## Human-presence credits found (Part II)
- **16.4 Embodied/situated knowledge (4 cr):** named stack `"built on n8n and Claude"`; measured `"Average measured gain across our clients: 7 hours per week per employee"`; `"score your AI maturity out of 24"`; FAQ5 `"offices in Paris (75008) and Rabat (Agdal)"`. Load-bearing — delete them and the differentiation collapses.
- **16.2 Stakes & exposure (4 cr):** `"four guarantees written into the contract"`, `"full IP transfer"`, and the capacity cap `"We take on at most 3 new clients a month"` — reputation and revenue staked in plain view.
- **16.7 Specific address (3 cr):** `"We take on at most 3 new clients a month. Worth spending them on the right fit."` — scarcity-as-taste, deliberately shrinks the audience. FAQ1 also answers a specific reader's agency-vs-consultancy objection in their vocabulary.
- **16.1 Temporal selfhood / continuity (2 cr):** GEO block names the founder (`"founded by Othmane Halim"`) and cumulative track record (`"200+ AI systems deployed across 50+ companies"`) — the firm exists in time.
- **16.8 Idiosyncrasy fingerprint (recorded, not double-counted):** private signature `"systems, not slides"` used consistently.
- **Layer 13 protection:** the shared-table note ("owned by homepage, do not fork") and canonical-owner FAQ tags are correctness/dedup markers — not penalized.

## Concrete remediation
- The risk is corpus sameness, not this page. Do NOT synonym-swap the "X, not Y" frame — **delete** the frame on one of the two restatements and state the fact flat (e.g., "Three families of AI systems, each shipped to production and documented").
- `"strong leverage for AI"` → replace with a concrete payoff ("documentation-heavy teams see the biggest measured gains").

## Repeated devices seen on this page (for synthesis pass)
- **"systems, not slides" / "Systems in production, not demos"** — the signature tagline (shared with homepage).
- **"whether you work with us or not"** (CTA subtitle) — universal CTA reassurance closer (shared with homepage).
- **"30 minutes … leave with your first 3 AI quick wins"** (CTA subtitle) — templated CTA line (shared with homepage).
- **"at most 3 new clients a month"** — scarcity device (shared with homepage).
- **"a company that runs without us at six months" / "trains your teams to run them without us"** — the autonomy closer.
- **"four guarantees written into the contract" / "full IP transfer"** — recurring guarantee pair.
- **Rule-of-three phase names: AI Scan / AI Engine / AI Champions; Audit / Build / Scale.**
- **Canonical figures: 7h/week, maturity out of 24, 200+ systems, 50+ companies, 9.6/10, 1–2 systems/month.**
