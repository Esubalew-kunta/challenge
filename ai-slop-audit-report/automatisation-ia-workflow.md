# Anti-AI-Slop Audit — Automatisation IA workflow (`/automatisation-ia-workflow` → `/ai-automation`)

## Net score: SDS − HPC = ~ −4
- **Word count (AIM-authored EN prose; excl. scaffolding/tables/rationale):** ~830
- **SDS (slop density /1,000w):** ~10
- **HPC (human presence credits /1,000w):** ~14 (hard cap 20)
- **Net:** ≈ **−4**
- **Layer 14 verdict:** **CLEAN — ship.** Among the strongest pages in the batch. Lexically near-immaculate (no delve/landscape/leverage/seamless/robust/streamline). Every section passes Zero-Knowledge: named stack with candid rankings, measured KPIs, stated trade-offs with verdicts. The only real issue is the shared CTA closer and the recurring "X, not Y" frame.

## Top tells (verbatim, with layer/weight)
1. **Layer 2.1 — negative parallelism, house style (3 pts, recurrence).** `"systems shipped to production, not licences resold"` (intro); `"we don't sell licences or prototypes: we ship systems to production"` (p3); GEO `"not resold as licences."` Load-bearing each, but the frame is the corpus signature — density is the concern.
2. **Layer 2.5 — colon-headline (2 pts).** `"Its limits: cost at volume and restricted logic."` (Zapier card). One instance; punchy and factual, borderline acceptable.
3. **Layer 1 Tier B / intensifier (1 pt).** `"What an automated workflow actually looks like"` + `"What actually gets automated"` — "actually" as emphasis in two adjacent section titles. Cut one; the word is doing throat-clearing work ("actually" implies a strawman nobody stated).
4. **Layer 2.14 — em-dash appositive punch (1 pt).** `"The maths is quick."` and `"— no manual re-keying."` — short-payload dashes for beat. Effective, low density, watch only.

## Human-presence credits found (Part II)
- **16.8 Idiosyncrasy / aesthetic verdict as taste (3 cr):** the Zapier card ends `"Honestly, rarely our first pick for real business processes."` and the stack states an explicit ranking (n8n > Make > Zapier). Taste asserted as taste — slop can't rank.
- **16.2 Stakes & exposure (4 cr):** `"What isn't measured gets dropped."`; `"We have no exclusivity with anyone."`; `"a bad process automated is still a bad process."`; IP transfer in full. Admissions against interest and hard verdicts.
- **16.4 Embodied/situated knowledge (4 cr):** `"a tool with no process behind it sits unused after three weeks"`; `"cost that doesn't explode with volume"`; the six named process families; `"n8n. Open source, self-hosted … native connection to AI models."` These carry information, not decoration.
- **16.7 Specific address (2 cr):** `"If your teams spend hours a week on them, there's a system to build."` — models the reader's own situation rather than broadcasting.
- **Layer 13 protection:** the "REUSE / owned by homepage" table note and the struck-through `~~60–80% absorption~~` line ("dropped to avoid a [to validate] claim") are honesty/dedup markers — **not penalized**; the dropped-stat note is a positive integrity signal.

## Concrete remediation
- De-duplicate "actually": rename one section title (e.g., `"What an automated workflow looks like"` — drop "actually").
- The "X, not Y" frame: keep p3's `"we ship systems to production … not licences or prototypes"` (the sharpest); flatten the intro and GEO restatements to plain assertions so the tic doesn't stack.
- CTA subtitle is verbatim-identical to homepage/agence-ia — vary it per page or accept it as a deliberate standard (flag for synthesis).

## Repeated devices seen on this page (for synthesis pass)
- **"30 minutes … leave with your first 3 AI quick wins — whether you work with us or not."** (CTA subtitle) — identical across homepage, agence-ia, this page. The universal CTA closer.
- **"systems to production, not licences/prototypes/demos"** — signature contrast tagline.
- **"IP transferred to you in full" / "the IP is entirely yours"** — recurring guarantee line.
- **"1–2 systems a month," "two hours of weekly training," "autonomy at six months"** — canonical offer figures.
- **"7 hours saved per week per employee"** — canonical measured figure.
- **Gerund/rule-of-three phase names: Mapping / ROI scoring / Build / Measurement.**
