# Anti-AI-Slop Audit — Audit IA entreprise (`/audit-ia-entreprise` → `/ai-readiness-assessment`)

## Net score: SDS − HPC = ~ 0
- **Word count (AIM-authored EN prose; excl. scaffolding/tables/rationale):** ~800
- **SDS (slop density /1,000w):** ~14
- **HPC (human presence credits /1,000w):** ~13 (hard cap 20)
- **Net:** ≈ **0**
- **Layer 14 verdict:** **CLEAN — ship (with one structural edit).** Passes Zero-Knowledge everywhere (named grid /24, refund threshold, staged roadmap). The page is dragged up mainly by two things: the highest "X, not Y" density in the batch and a genuine within-page redundancy (sections 4.2 and 4.3 restate the same four items). Fix the redundancy and it drops to a clear Clean.

## Top tells (verbatim, with layer/weight)
1. **Layer 3.7 / near-duplicate block (3 pts) — the real issue.** Sections 4.2 ("What an AI Scan does") and 4.3 ("Four steps in two weeks") list the *same four items* with near-verbatim copy: 4.2 `"We document each department's real workflows: who does what, with which tools, in how much time. The real work, not the org chart."` vs 4.3 `"We map each department's workflows: who does what, with which tools, in how long. The real work, not the org chart."` Same for interviews / maturity /24 / ExCom. The reader hits the identical content twice. **Remediation:** collapse to one section, or make 4.3 a genuine timeline (week 1 / week 2 with dates and who's in the room) instead of re-describing 4.2's components.
2. **Layer 2.1 — negative parallelism, highest density in batch (3 pts, doubled).** `"Decisions, not a report that gathers dust."` (4.2) and `"Decisions, not a dormant report."` (4.3); `"The real work, not the org chart."` (×2); `"Not an 80-page report that ends up in a drawer."`; `"the people who actually run the processes, not just the managers."` Load-bearing individually, but four-plus instances on one page is the corpus tic at full volume.
3. **Layer 2.5 — colon/equation headline (2 pts).** Guarantee card title `"No clear roadmap = refunded in full"` and `"For each: the process concerned, the estimated gain…"`. Punchy but recurring device.

## Human-presence credits found (Part II)
- **16.2 Stakes & exposure (4 cr):** `"If the assessment doesn't produce a clear roadmap with at least 3 high-ROI use cases, you're refunded 100%. It's written into the contract."` and `"An audit that doesn't let you decide is worth nothing — we take that risk instead of you."` Reputation and money staked.
- **16.4 Embodied/situated knowledge (4 cr):** the proprietary `"maturity score out of 24"` grid (tools/data/skills/usage); `"an audit that drags on for three months produces an already-stale report"`; FAQ2 `"the gap between the official process and the real work — where the best gains hide."` First-hand method detail.
- **16.8 Idiosyncrasy / taste (2 cr):** `"Not an 80-page report that ends up in a drawer."` — aesthetic verdict on classic consulting, asserted as taste.
- **16.7 Specific address (2 cr):** FAQ4 `"any company of 50+ people has automatable processes — the only question is which ones to prioritise."` — sizes the reader, doesn't broadcast.
- **Layer 13 protection:** the inline reconcile note `"(NOTE: /diagnostic-ia OG says /20 → reconcile)"` and `"maturity score /24 (per page.tsx + llms strategy)"` are honesty/consistency flags in scaffolding, not published prose — **not penalized**; they're a positive integrity signal.

## Concrete remediation
- **Kill the 4.2/4.3 duplication** (the one substantive fix): merge into a single "What the AI Scan does, week by week" section. This removes the redundancy AND one copy of "The real work, not the org chart" / "Decisions, not a dormant report."
- Thin the "X, not Y" frame to the two sharpest ("Not an 80-page report in a drawer" + the refund line); flatten the rest to plain assertions — do not synonym-swap.
- Confirm the /24 vs /20 grid inconsistency is resolved before publish (flagged in scaffolding).

## Repeated devices seen on this page (for synthesis pass)
- **"The real work, not the org chart."** — this page's signature contrast (used twice here).
- **"Decisions, not a report that gathers dust / dormant report / 80-page report in a drawer."** — the anti-consulting-deliverable trope.
- **"30 minutes … first 3 AI quick wins — whether you work with us or not."** (CTA subtitle) — the universal CTA closer (identical across pages).
- **"maturity score out of 24," "at least 3 high-ROI use cases," "3-, 6-, 12-month horizons," "refunded 100%," "30-day impact guarantee"** — canonical figures.
- **"usable / everything is yours, with or without us"** — recurring no-lock-in reassurance.
