# Anti-AI-Slop Audit — AI Operating System (`/ai-operating-system`)

## Net score: SDS − HPC = ~ 0
- **Word count (AIM-authored EN prose; excl. scaffolding/tables/rationale):** ~720
- **SDS (slop density /1,000w):** ~14
- **HPC (human presence credits /1,000w):** ~14 (hard cap 20)
- **Net:** ≈ **0**
- **Layer 14 verdict:** **CLEAN — ship.** A concept page for a brand-coined term that earns the coinage with a concrete failure-state, a defensible 4-layer architecture, and first-hand dogfooding. No automatic-failure class. The concern is again density of the "X, not Y" frame (this page leans on it structurally — the entire thesis is "OS, not tools"), which is load-bearing here but stacks toward tic. One thinning pass.

## Top tells (verbatim, with layer/weight)
1. **Layer 2.1 — negative parallelism, structural (3 pts, recurrence).** The frame is the page's spine: `"Not a pile of tools. An OS…"` · `"An OS, not tools."` · `"tools get added, an OS fits together"` · `"We don't sell a method read somewhere. We duplicate ours."` · `"We replace nothing, we plug in."` · `"a component of the system, not a duplicate."` · `"Your competitors buy tools. Install a system."` Seven instances. Because the whole thesis IS a contrast (OS vs tools), some are unavoidable — but seven is over-deployment. Keep the thesis pair (`"An OS, not tools."` + `"tools get added, an OS fits together"`); flatten the rest.
2. **Layer 2.5 — colon-headline (2 pts).** `"It's our own OS."`, `"The difference fits in one line: tools get added…"`, and title-case `"AI Operating System: Run Your Company on AI"`. Recurring device.
3. **Layer 2.4 — rule-of-three (1 pt, borderline).** `"same layers, same agents, same dashboards"` (anaphora + triad). Effective, but combined with the OS/tools contrasts the page has a high slogan-cadence.

## Human-presence credits found (Part II)
- **16.4 Embodied/situated knowledge — dogfooding (4 cr):** `"It's what runs AI Makers, and what we install at our clients."` and `"AI Makers runs on the system we install for you: same layers, same agents, same dashboards. We duplicate ours."` Plus the concrete failure-state: `"ten subscriptions across ten silos, and the work still moves by copy-paste"`; `"three automations asleep in a corner."` Information-bearing.
- **16.8 Idiosyncrasy fingerprint (3 cr):** the private coinage (the 4-layer AI OS: Data / Systems / Agents / Control) used consistently, and the signature inverted-syntax line `"You run on instinct a system you can't see."` — a stable voice quirk, not costume.
- **16.2 Stakes & exposure (3 cr):** FAQ1 `"You do, in full … Zero dependency — it's written into the contract."`; full client ownership; the guarantees reuse. Reputation staked on the exit terms.
- **16.7 Specific address (2 cr):** FAQ3/4 answer real objections in the reader's words — `"Does it replace our current tools?"` → `"We replace nothing, we plug in."`; `"We already have automations. Do we start from scratch?"` → keeps what works.
- **Layer 13 protection:** `[to validate]` on the 6-people/team-of-40 stat and the Gepromed EN-translation approval; REUSE notes on fleet/guarantees/testimonials — honesty and dedup markers, **not penalized.**

## Concrete remediation
- Thin "OS, not tools" from 7 to ~3 instances. The thesis contrast can stay; the CTA (`"Your competitors buy tools. Install a system."`), `"We replace nothing, we plug in"`, and `"not a duplicate"` are the ones to flatten so the reader doesn't get the same antithesis seven times. Do not synonym-swap.
- Confirm the 6-people/team-of-40 figure and Gepromed EN quote approval before publish (already flagged).

## Repeated devices seen on this page (for synthesis pass)
- **"An OS, not tools." / "buy tools, install a system"** — this page's structural signature contrast.
- **"We replace nothing, we plug in."** — the connections tagline (shared with homepage).
- **"We run on it ourselves" / dogfooding "6 people, output of a team of 40"** — the mechanism proof (shared with homepage + ai-transformation).
- **"whether you work with us or not" / "first 3 AI quick wins"** — universal CTA closer.
- **"At most 3 new clients a month"** — scarcity badge.
- **"Zero dependency" / "written into the contract" / full ownership** — exit-guarantee line.
- **AI Scan / AI Engine / AI Champions · phase durations (1–2 weeks / 3–6 months / from month 3)** — canonical phase naming.
