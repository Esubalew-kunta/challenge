# AI-Slop Audit — formation-ia-entreprise--creation-publicite-ia (EN Proposed copy)

## Net score: SDS − HPC = 8 − 11 = **-3** (per 1,000 words)

**Verdict: Clean (Layer 14, 0–8 band).** Action: ship. Distinctive creative-stack anchor (Nano Banana + Weavy) keeps it out of generic "AI content" territory. Honor `[to validate]` on "x5 faster" and the funding line.

### Substance (Layer 5): PASS
The two-tool split is a real, teachable structure (Nano Banana for generation → Weavy for the pipeline, brief to delivery). The load-bearing tension — "keeping a consistent art direction when AI is in the loop" — recurs with a clear verdict in the FAQ ("No. AI speeds up production, but art direction stays with your team."), which is a genuine 5.10 "the missing no."

### Top tells (verbatim + layer)
- "without losing art direction" / "without giving up quality or brand consistency" — Layer 2.1 negative-frame device, twice. ~2 pts.
- "Stay true to brand identity" / "The prompts behind quality visuals" — light marketing gloss. ~1 pt.
- Rule-of-three "social, print, web" — Layer 2.4, concrete. ~1 pt.
- Uniform sibling FAQ arc + shared boilerplate — Layer 3.6 / Tier C. ~2 pts.
- "x5 faster production cycles" — Layer 9.2 precision without a source, but correctly tagged `[to validate]` → not a fabrication. ~1 pt (pending validation, not penalised as fab).

### Credits (Layer 16) — load-bearing
- 16.4 situated knowledge (~3 cr): the generation→pipeline tool split and "keep art direction consistent" workflow are practitioner specifics.
- 16.2 / 5.10 verdict (~2 cr): FAQ refuses the flattering claim ("Does AI replace art direction? No") — an admission that limits the sell.
- 16.7 specific address (~1 cr): "It's a team program, not an individual course."
- 16.11 honesty (~2 cr): `[to validate]` on x5; funding flagged unsourced.

### Synthetic-anecdote check (17.4): N/A — program register.

### Remediation
- Pick one "without losing X" frame and cut the duplicate. State the x5 claim only once it is validated.

### Repeated devices seen on this page (formations corpus map)
- Same formation arc + verbatim boilerplate (trainer line, catalogue form, "Book a diagnostic", "we'll map your … workflow and scope the format").
- Uniform FAQ set (who-for / which-tools / does-AI-replace-X); GEO "team program, not an individual course" closer shared across siblings.
