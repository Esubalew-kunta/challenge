# Anti-AI-Slop Audit — AI Transformation (`/ai-transformation`)

## Net score: SDS − HPC = ~ −1
- **Word count (AIM-authored EN prose; excl. scaffolding/tables/rationale):** ~1,450
- **SDS (slop density /1,000w):** ~14
- **HPC (human presence credits /1,000w):** ~15 (hard cap 20)
- **Net:** ≈ **−1**
- **Layer 14 verdict:** **CLEAN — ship.** The batch's richest page for human presence: first-hand dogfooding ("we run on what we sell" with named internal systems), current technical detail (MCP), a cost-of-delay thesis carried through the whole page, and honest uncertainty calibration. No automatic-failure class; MCP and Sage +70% claims are accurate/sourced, not fabricated. The recurring "X, not Y" frame and colon-headlines are present but diluted across 1,450 words.

## Top tells (verbatim, with layer/weight)
1. **Layer 2.1 — negative parallelism, house style (3 pts, recurrence).** `"It isn't a technology problem. It's a system problem."` · `"A tool changes nothing: what changes everything is a system configured for how you work."` · `"Not a method read in a white paper: our own way of working."` · `"the format that makes teams autonomous, not the one-day seminar."` · `"written into the contract, not onto a slide."` · `"Not a promise: a measured result."` · `"Our job isn't to ride the wave. It's to build…"` Individually load-bearing, but the frame recurs ~7× — corpus tic. Thin to the two sharpest.
2. **Layer 2.5 — colon-headline (2 pts, multiple).** `"The result: a team of 6 that produces like a team of 40."` · `"What lasts: regular practice on your real cases…"` · `"Your AI department, outsourced. Three phases. Zero risk."` Recurring device across the site.
3. **Layer 1 Tier A — mild metaphor (1 pt).** `"Our job isn't to ride the wave"` — "ride the wave" is a soft cliché; the surrounding verdict rescues it. Low weight.
4. **Layer 2.4 — rule-of-three/four (watch, not scored).** `"AI reads, sorts, drafts, reconciles."`; `"Four rules. No exceptions."`; `"Three phases. Zero risk."` Enumerative and concrete (real lists), so no penalty — but slogan cadence is dense.

## Human-presence credits found (Part II)
- **16.4 Embodied/situated knowledge — dogfooding (4 cr):** `"AI Makers runs on its own systems. The morning decision brief, the analysis of every sales call, the prep for every meeting, the health tracking of every client engagement: all produced by the agents we build."` Named internal systems that carry evidentiary weight — delete them and the "we run on what we sell" proof collapses.
- **16.2 Stakes & exposure (5 cr):** the guarantee terms stated inline in each phase gain — `"Otherwise the audit is refunded."`; `"Otherwise we keep going for free until they run."`; and the four-rule model (`"Four guarantees written into the contract, not onto a slide."`). Money and reputation staked.
- **16.5 Live cognition / honest calibration (2 cr):** the 24-month horizon `"Nobody knows the exact timeline. The direction isn't in doubt."` — genuine open uncertainty landed with a verdict, not a hedge sandwich.
- **16.9 Temporal & cultural anchoring (2 cr):** `"MCP (Model Context Protocol, the open standard launched by Anthropic and adopted by OpenAI and Google)"` and `"AI Makers is an Anthropic Partner"` — datable, current-scene detail the writer accepts will age. Accurate, not fabricated.
- **16.1 Temporal selfhood / continuity (2 cr):** `"We run on what we sell."` + the compounding lead/lag thesis binds the argument to a firm existing in time.
- **Layer 13 protection (extensive, all honest):** `[to validate]` tags on the 6-people/team-of-40 figure, the $80k+/yr converted figure, logo EN-use, and testimonial EN-approval; plus the FR→EN currency-conversion note. All are integrity markers — **not penalized**; positive signal. Guarantees/testimonials REUSE notes are correct dedup.

## Concrete remediation
- Thin the "X, not Y" frame from ~7 to ~3: keep the thesis `"It isn't a technology problem. It's a system problem."` and `"Not a promise: a measured result."`; flatten the rest to plain assertions. Do not synonym-swap.
- Resolve the 60–80% figure's status: it is tagged `[to validate]` on the homepage but presented flat here (`"60–80% of repetitive work … is absorbable by AI"`). Same claim should carry the same honesty tag until validated (consistency, not a prose-slop issue).
- Confirm the $80k+/yr and 6-people/40 figures before publish (already flagged).

## Repeated devices seen on this page (for synthesis pass)
- **"written into the contract, not onto a slide." / "Not a promise: a measured result."** — the anti-hype signature tagline.
- **"We run on what we sell" / dogfooding "team of 6 produces like a team of 40"** — the mechanism proof (shared with homepage objection #3).
- **"whether you work with us or not" / "first 3 AI quick wins"** (final CTA) — universal CTA closer.
- **"At most 3 new clients a month" / "physically limited"** — scarcity device.
- **"Zero dependency" / "everything stays with you: systems, code, playbooks"** — exit-guarantee line.
- **AUDIT / BUILD / SCALE · AI Scan / AI Engine / AI Champions · "Three phases. Zero risk."** — rule-of-three phase naming.
- **Canonical figures: 60–80%, 2h/week, /24 grid (6 axes), 1–2 systems/month, autonomy at 6 months, 7h/week, 1,500+ automations, 200+/50+/2,500+.**
