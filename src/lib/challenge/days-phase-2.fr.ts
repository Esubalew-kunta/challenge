/**
 * Phase 2 — Le rendre vôtre. Jours 11 à 20.
 *
 * Traduction en cours. Les jours déjà écrits sont dans ce tableau ; les autres
 * renvoient automatiquement vers la page anglaise, parce qu'un lien mort coûte
 * plus cher au lecteur qu'une page dans la mauvaise langue.
 *
 * Mêmes règles que la phase 1 : numéros de jour identiques à l'anglais, ordre
 * des options de quiz identique, commandes non traduites.
 */

import type { Day } from "./types";

export const PHASE_2_DAYS_FR: Day[] = [];
